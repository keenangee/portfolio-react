const ProjectsPicker = () => {
  return (
    <section
      id="projects"
      className="min-h-screen flex content-center items-center"
    >
      <h1>ProjectsPicker</h1>
    </section>
  );
};

export default ProjectsPicker;
